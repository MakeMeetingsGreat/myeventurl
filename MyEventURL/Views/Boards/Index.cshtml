@model IEnumerable<MyEventURL.Models.Board>

@{
    ViewBag.Title = "Index";
    string Events = "1,2,3";
    var EventsList = Events.Split(',');
    Events = "";
    Events = EventsList.ToString();
    Output.Write(EventsList[0]);
}
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Created)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Events)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.View)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Created)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Events)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.View)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BoardID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BoardID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BoardID })
        </td>
    </tr>
}

</table>
